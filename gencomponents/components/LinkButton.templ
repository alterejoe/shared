package components
import "github.com/alterejoe/shared/structs"

templ PrimaryLinkButton(props *structs.Link) {
    <a	
		if props.Common.ID != "" {
			id={ props.Common.ID }
		}
		if props.Href != "" {
			href={ templ.SafeURL(props.Href) }
		}
		if props.Common.Class != "" {
			class={ "py-2 px-4 text-xl cursor-pointer transition-colors bg-primary hover:bg-primary/90 " + props.Common.Class }
		} else {
			class="py-2 px-4 text-xl cursor-pointer transition-colors bg-primary hover:bg-primary/90"
		}
	>
		if props.Common.Value != "" {
			{ props.Common.Value }
		} else {
			{ children... }
		}
	</a>
}

templ SecondaryLinkButton(props *structs.Link) {
    <a	
		if props.Common.ID != "" {
			id={ props.Common.ID }
		}
		if props.Href != "" {
			href={ templ.SafeURL(props.Href) }
		}
		if props.Common.Class != "" {
			class={ "py-2 px-4 text-xl cursor-pointer transition-colors bg-secondary hover:bg-secondary/90 " + props.Common.Class }
		} else {
			class="py-2 px-4 text-xl cursor-pointer transition-colors bg-secondary hover:bg-secondary/90"
		}
	>
		if props.Common.Value != "" {
			{ props.Common.Value }
		} else {
			{ children... }
		}
	</a>
}

templ TertiaryLinkButton(props *structs.Link) {
    <a	
		if props.Common.ID != "" {
			id={ props.Common.ID }
		}
		if props.Href != "" {
			href={ templ.SafeURL(props.Href) }
		}
		if props.Common.Class != "" {
			class={ "py-2 px-4 text-xl cursor-pointer transition-colors bg-tertiary hover:bg-tertiary/90 " + props.Common.Class }
		} else {
			class="py-2 px-4 text-xl cursor-pointer transition-colors bg-tertiary hover:bg-tertiary/90"
		}
	>
		if props.Common.Value != "" {
			{ props.Common.Value }
		} else {
			{ children... }
		}
	</a>
}

templ QuaternaryLinkButton(props *structs.Link) {
    <a	
		if props.Common.ID != "" {
			id={ props.Common.ID }
		}
		if props.Href != "" {
			href={ templ.SafeURL(props.Href) }
		}
		if props.Common.Class != "" {
			class={ "py-2 px-4 text-xl cursor-pointer transition-colors bg-quaternary hover:bg-quaternary/90 " + props.Common.Class }
		} else {
			class="py-2 px-4 text-xl cursor-pointer transition-colors bg-quaternary hover:bg-quaternary/90"
		}
	>
		if props.Common.Value != "" {
			{ props.Common.Value }
		} else {
			{ children... }
		}
	</a>
}

templ AccentLinkButton(props *structs.Link) {
    <a	
		if props.Common.ID != "" {
			id={ props.Common.ID }
		}
		if props.Href != "" {
			href={ templ.SafeURL(props.Href) }
		}
		if props.Common.Class != "" {
			class={ "py-2 px-4 text-xl cursor-pointer transition-colors bg-accent hover:bg-accent/90 " + props.Common.Class }
		} else {
			class="py-2 px-4 text-xl cursor-pointer transition-colors bg-accent hover:bg-accent/90"
		}
	>
		if props.Common.Value != "" {
			{ props.Common.Value }
		} else {
			{ children... }
		}
	</a>
}

templ UniqueLinkButton(props *structs.Link) {
    <a	
		if props.Common.ID != "" {
			id={ props.Common.ID }
		}
		if props.Href != "" {
			href={ templ.SafeURL(props.Href) }
		}
		if props.Common.Class != "" {
			class={ "py-2 px-4 text-xl cursor-pointer transition-colors bg-unique hover:bg-unique/90 " + props.Common.Class }
		} else {
			class="py-2 px-4 text-xl cursor-pointer transition-colors bg-unique hover:bg-unique/90"
		}
	>
		if props.Common.Value != "" {
			{ props.Common.Value }
		} else {
			{ children... }
		}
	</a>
}

templ AttentionLinkButton(props *structs.Link) {
    <a	
		if props.Common.ID != "" {
			id={ props.Common.ID }
		}
		if props.Href != "" {
			href={ templ.SafeURL(props.Href) }
		}
		if props.Common.Class != "" {
			class={ "py-2 px-4 text-xl cursor-pointer transition-colors bg-attention hover:bg-attention/90 " + props.Common.Class }
		} else {
			class="py-2 px-4 text-xl cursor-pointer transition-colors bg-attention hover:bg-attention/90"
		}
	>
		if props.Common.Value != "" {
			{ props.Common.Value }
		} else {
			{ children... }
		}
	</a>
}

templ TransparentLinkButton(props *structs.Link) {
    <a	
		if props.Common.ID != "" {
			id={ props.Common.ID }
		}
		if props.Href != "" {
			href={ templ.SafeURL(props.Href) }
		}
		if props.Common.Class != "" {
			class={ "py-2 px-4 text-xl cursor-pointer transition-colors bg-transparent hover:bg-transparent/90 " + props.Common.Class }
		} else {
			class="py-2 px-4 text-xl cursor-pointer transition-colors bg-transparent hover:bg-transparent/90"
		}
	>
		if props.Common.Value != "" {
			{ props.Common.Value }
		} else {
			{ children... }
		}
	</a>
}

